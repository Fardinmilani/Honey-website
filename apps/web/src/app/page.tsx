/**
 * `/` is handled by middleware with a 307 locale redirect.
 * This page exists only as an App Router anchor and should not render content.
 */
export default function RootPage() {
  return null;
}

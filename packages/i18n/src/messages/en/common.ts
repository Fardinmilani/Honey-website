import type { CommonMessages } from '../types.js';

export const common = {
  brandName: 'Honey',
  skipToContent: 'Skip to content',
  loading: 'Loading',
  language: 'Language',
} as const satisfies CommonMessages;

{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["build", "^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["build", "^build"],
      "outputs": [],
      "cache": false,
      "passThroughEnv": ["TEST_DATABASE_ADMIN_URL"]
    }
  }
}
